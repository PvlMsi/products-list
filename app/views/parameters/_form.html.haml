= form_with(model: parameter, local: true) do |form|
  - if parameter.errors.any?
    #error_explanation
      %h2
        = pluralize(parameter.errors.count, "error")
        prohibited this parameter from being saved:
      %ul
        - parameter.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name
  .field
    = form.label :data_type
    = form.number_field :data_type
  .field
    = form.label :options
    = form.text_field :options
  .actions
    = form.submit
